apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
    creationTimestamp: null
  labels:
    run: nginx
    name: nginx
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 2
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: nginx
        ports:
        - containerPort: 8081
